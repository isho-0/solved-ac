#include "problem_manager.h"

// cd /Users/enyo/Documents/GitHub/solved-ac && rm -rf build && mkdir build && cd build && cmake .. && make
int main() {
    ProblemManager manager;
    manager.run();
    return 0;
}
