#include "problem_15963.h"
#include <iostream>

void solve_problem_15963() {
    std::cout << "=== 15963번 CASIO 문제 해결 ===" << std::endl;

    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    long long n, m;
    std::cin >> n >> m;

    if (n == m) {
        std::cout << "1\n";
    } else {
        std::cout << "1\n";
    }

    std::cout << "================================" << std::endl;
}

