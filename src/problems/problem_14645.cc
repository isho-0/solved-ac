#include "problem_14645.h"
#include <iostream>

void solve_problem_14645() {
    std::cout << "=== 14645번 와이버스 부릉부릉 문제 해결 ===" << std::endl;

    int n, k;
    std::cin >> n >> k;

    int a, b;
    for (int i = 0; i < n; i++) {
        std::cin >> a >> b;
    }
    std::cout << "비와이\n";

    std::cout << "================================" << std::endl;
}
